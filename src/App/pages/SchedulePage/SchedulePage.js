import * as React from 'react';

import styles from './SchedulePage.modules.scss';

const SchedulePage = () => {
    return <div className={styles['schedule-page']}>
        schedule page
    </div>
}

export default SchedulePage;